class UserAgentActions {
  /**
   * @description
   * Load initial user agent data to the redux store and determine if user agent isDevice (mobile)
   */
  public static loadInitialUserAgent(): Function {
    return dispatch => {};
  }
}
